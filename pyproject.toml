[project]
name = "mazegen"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = [
	"pydantic==2.12.5"
]

[tool.poetry]
package-mode = false

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
